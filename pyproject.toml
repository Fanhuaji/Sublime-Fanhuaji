[project]
name = "Fanhuaji"
version = "0.0.0"
description = "繁化姬的 Sublime Text 插件"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
  "pydantic>=2.7,<3",
  "requests>=2.15.1,<3",
  "typing_extensions>=4.12",
]


[dependency-groups]
dev = [
  "mypy>=1.20",
  "ruff>=0.15",
  "types-requests",
]

[tool.uv]
default-groups = ["dev"]

[tool.mypy]
# ignore_missing_imports = False
check_untyped_defs = true
strict_optional = true
mypy_path = 'typings:stubs'
python_version = "3.14"

[[tool.mypy.overrides]]
module = ["plugin.libs.*"]
ignore_errors = true
ignore_missing_imports = true

[tool.pyright]
include = ['./']
exclude = [
  '**/__pycache__/',
  '**/node_modules/',
  # git-related
  '**/.git/',
  '**/br-*/',
  '**/branch-*/',
]
ignore = ['**/.venv', '**/libs', '**/vendor']
stubPath = 'typings'
pythonVersion = '3.14'

[tool.ruff]
preview = true
line-length = 120
target-version = 'py314'
exclude = [
  "*/libs/*",
  ".git",
  ".mypy_cache",
  ".venv",
  ".venv-*",
  "branch-*",
  "stubs",
  "tests/files",
  "typings",
  "vendor",
  "venv",
  "venv-*",
]

[tool.ruff.lint]
select = [
  "E",
  "F",
  "W",
  "I",
  "UP",
  "FURB",
  "SIM",
]
ignore = ["E203"]

[tool.ruff.lint.per-file-ignores]
"boot.py" = ["E402"]
